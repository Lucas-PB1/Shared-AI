# Testes

```bash
npm test
```

Parser de slug dos repositórios ligados (`tests/linked-project-slug.test.ts`).

```bash
npm run lint:ts       # tsc --noEmit (scripts Cursor + slug)
npm run lint:shell    # ShellCheck (skip suave se não instalado)
```
