# context.yaml sintético — fixture de review (exclusions export)
schema: 2
exclusions:
  - id: accepted-repo-pattern
    scope: "app/Vendor/**"
    decision: rejeitado
    reason: "padrão aceito pelo time neste escopo"
  - id: edge-preview
    scope: "preview/**"
    decision: nao-aplicavel
    reason: "só editor / preview"
  - id: still-pending
    scope: "**/*"
    decision: aceito
    reason: "não deve ir para exclusions.yaml no export"
