# Directory Update Log

## 2026-09-10

* **Creation**: Shared AI — pacote Cursor (skills, rules, bootstrap) e dashboard Next.js local (Auth no Docker + lista de repos em `~/.cursor/shared-ai/projects.json`). Schema: `profiles` + bucket `avatars`.
* **Update**: Escopo DND Work — skills Nest/FSD/Supabase/TypeORM/UI; perfis monorepo/nestjs/next/react; orquestrador padronizado; `/automations` (só sync), `/criar-skill`, `/criar-rule`; `CHANGELOG` + `npm run release`. Removidos review/CI automation drafts e stacks fora do escopo.
