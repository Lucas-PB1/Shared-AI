# Roteamento — complemento do projeto (Python)

Perfil: `python` (`npm run bootstrap -- <repo> --profile=python`).

Complementa `~/.cursor/SKILLS-ROUTING.md`.

## Stack

- Python — `pyproject.toml`, `requirements.txt`, `setup.py`, `*.py`

## Skills priorizadas

| Skill | Reforçar quando |
| --- | --- |
| `python` | Scripts, libs, CLI, tipagem, async, módulos |
| `testing` | pytest, unittest, `test_*.py` |
| `security` | Input, secrets, subprocess, paths |

## Rule do projeto

- `.cursor/rules/python-project.mdc` — editável e versionável neste repo
