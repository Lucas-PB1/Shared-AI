# Roteamento — complemento do projeto (React)

Perfil: `react` (`npm run bootstrap -- <repo> --profile=react`).

Complementa `~/.cursor/SKILLS-ROUTING.md`.

## Stack

- React — `package.json` (`react`, `react-dom`), `*.{tsx,jsx}`

## Skills priorizadas

| Skill | Reforçar quando |
| --- | --- |
| `react` | Componentes, hooks, estado, forms |
| `typescript` | Tipos, props, APIs (se TS no repo) |
| `tailwind` | Estilização utility-first (se no manifesto) |
| `accessibility` | UI interativa, forms, navegação |
| `testing` | RTL, Vitest/Jest |

## Rule do projeto

- `.cursor/rules/react-project.mdc` — editável e versionável neste repo
