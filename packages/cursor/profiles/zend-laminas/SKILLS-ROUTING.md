# Roteamento — complemento do projeto (Zend / Laminas)

Perfil: `zend-laminas` (`npm run bootstrap -- <repo> --profile=zend-laminas`).

Complementa `~/.cursor/SKILLS-ROUTING.md`.

## Stack

- Laminas / Zend Framework — `composer.json` (`laminas/*`, `zendframework/*`), `module/`, `Module.php`

## Skills priorizadas

| Skill | Reforçar quando |
| --- | --- |
| `zend-laminas` | MVC, módulos, ServiceManager, forms |
| `php` | Classes PHP fora do convenção Laminas |
| `testing` | PHPUnit, `tests/` |
| `security` | Input filters, auth, XSS |

## Rule do projeto

- `.cursor/rules/zend-laminas-project.mdc` — editável e versionável neste repo
