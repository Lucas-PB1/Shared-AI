# Roteamento — complemento do projeto (HubSpot CMS)

Perfil: `hubspot` (`npm run bootstrap -- <repo> --profile=hubspot`).

Complementa `~/.cursor/SKILLS-ROUTING.md`.

## Stack

- HubSpot developer project — `hsproject.json`, `@hubspot/cms-components`, `@hubspot/cli`

## Skills priorizadas

| Skill | Reforçar quando |
| --- | --- |
| `hubspot-cli` | `hs project upload`, deploy, dev server, auth |
| `react` | Componentes CMS React |
| `typescript` | Módulos TS/TSX do projeto HubSpot |
| `testing` | Testes de componentes |

## Rule do projeto

- `.cursor/rules/hubspot-project.mdc` — editável e versionável neste repo

## MCP HubSpot

- Rule `skills-orchestrator-hubspot.mdc` — verifica MCP na primeira tarefa HubSpot
- Command `/hubspot-mcp` — instalação explícita do servidor HubSpotDev
