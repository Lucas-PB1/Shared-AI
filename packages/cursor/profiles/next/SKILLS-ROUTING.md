# Roteamento — complemento do projeto (Next.js)

Perfil: `next` (`npm run bootstrap -- <repo> --profile=next`).

Complementa `~/.cursor/SKILLS-ROUTING.md`.

## Stack

- Next.js — `package.json` (`next`), `app/` ou `pages/`, `next.config.*`

## Skills priorizadas

| Skill | Reforçar quando |
| --- | --- |
| `next` | App Router, RSC, layouts, metadata, cache, Server Actions |
| `react` | Componentes, hooks, forms (Client Components) |
| `typescript` | Tipos, props, APIs (se TS no repo) |
| `tailwind` | Estilização utility-first (se no manifesto) |
| `testing` | Vitest/Jest, testes de rotas e componentes |

## Rule do projeto

- `.cursor/rules/next-project.mdc` — editável e versionável neste repo
