# Clients SSR

- `createBrowserClient` — Client Components / browser
- `createServerClient` — Server Components, Route Handlers, Server Actions (cookies read/write)
- Middleware / `proxy`: refrescar sessão antes de rotas protegidas
- Não compartilhar uma única instância mutável entre request isolation no servidor
