# Views e contratos

- Views `v_*` como API de leitura para TypeORM `@ViewEntity`
- Colunas e tipos estáveis; breaking change = nova view ou versionamento explícito
- Documentar slugs/ids usados como contrato entre BCs
- Grants mínimos para roles da aplicação
