# Jest + NestJS

- `@nestjs/testing`: `Test.createTestingModule` com providers mockados
- E2E HTTP: `supertest` + app Nest; `DATABASE_URL` documentada se integrar DB
- Preferir testar controller/handler com deps mocked para unit rápido
- Cobertura: `test:cov` do projeto; não baixar threshold sem acordo
