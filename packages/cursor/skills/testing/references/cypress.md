# Cypress e2e

- Fluxos críticos (login, criar recurso, caminho feliz da feature)
- `data-cy` / role queries estáveis; evitar seletores de CSS frágeis
- `start-server-and-test` (ou equivalente) para subir app antes do run
- Isolar estado (user de teste, reset) — não depender de dados manuais locais
