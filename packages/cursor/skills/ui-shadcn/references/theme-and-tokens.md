# Temas e tokens

- Preferir CSS variables / tokens do projeto (`next-themes` se dark mode)
- Cores semânticas (primary, muted, destructive) em vez de hex solto no JSX
- Animações leves (`tw-animate-css`) respeitando `prefers-reduced-motion` quando possível
