# Primitives e CVA

- Anatomy: root + parts; props `variant` / `size` tipadas
- `cva()` para classes; `cn()` para merge condicional
- Forward refs quando o primitive exige
- Não quebrar a API pública do componente shared por um uso pontual — estender variante
