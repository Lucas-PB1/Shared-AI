# Projetos e env

- Um projeto Vercel por app (front e API separados é comum)
- Env: `NEXT_PUBLIC_*` no front; secrets só server
- Preview deployments para PR quando o time usar
- Documentar URLs de callback Auth (Supabase) por ambiente
