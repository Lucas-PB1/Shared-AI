# Pacote Cursor

Instalado com `npm run setup:skills`.

Rules do orquestrador, skills e commands → `~/.cursor/` (global). Projeto via `link-project`.

Bootstrap: `npm run bootstrap -- <repo> [--profile=laravel|react|next|python|zend-laminas]`. Detach: `npm run detach -- <repo>`. Testes: `npm run test`.
